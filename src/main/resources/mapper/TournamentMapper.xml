<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.TournamentMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.entity.Tournament">
        <id column="id" property="id" />
        <result column="series_id" property="seriesId" />
        <result column="level_code" property="levelCode" />
        <result column="host_user_id" property="hostUserId" />
        <result column="champion_points_ratio" property="championPointsRatio" />
        <result column="status" property="status" />
        <result column="start_date" property="startDate" />
        <result column="end_date" property="endDate" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, series_id, level_code, host_user_id, champion_points_ratio, status, start_date, end_date, created_at, updated_at
    </sql>

</mapper>
