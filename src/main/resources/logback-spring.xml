<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 使用系统属性设置启动时间戳 -->
    <property name="LOG_PATH" value="logs"/>
    <property name="START_TIME" value="${START_TIME:-unknown}"/>
    <property name="LOG_FILE" value="${LOG_PATH}/curling-masters_${START_TIME}.log"/>
    <property name="ERROR_LOG_FILE" value="${LOG_PATH}/curling-masters-error_${START_TIME}.log"/>

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}{Asia/Shanghai} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件输出 - 所有日志 -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}{Asia/Shanghai} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 错误日志文件输出 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.FileAppender">
        <file>${ERROR_LOG_FILE}</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}{Asia/Shanghai} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <!-- Spring Boot的特殊日志配置 -->
    <logger name="org.springframework.boot" level="INFO"/>
    <logger name="org.springframework.security" level="DEBUG"/>
    <logger name="org.thymeleaf" level="DEBUG"/>
    <logger name="com.example" level="DEBUG"/>
    <logger name="org.apache.ibatis" level="DEBUG"/>

    <!-- 根日志器 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>
</configuration>
